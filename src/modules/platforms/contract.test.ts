import {describe,it,expect} from "vitest"; import {globalPreflight,validatePlatform} from "./contract"; import type { MediaMetadata } from "../media/model";
const media:MediaMetadata={width:1080,height:1920,duration:30,container:"mp4"};
const account={connected:true,eligible:true,scopes:["youtube.upload"],dynamic:{}};
describe("platform preflight",()=>{it("requires explicit YouTube title/privacy",()=>{const r=validatePlatform("youtube",{caption:"c",config:{enabled:true,title:"",privacy:"public"},media,account,capabilities:{}});expect(r.reasons).toContain("Título obligatorio")});it("enforces title limit and short eligibility",()=>{const r=validatePlatform("youtube",{caption:"",config:{enabled:true,title:"x".repeat(101),privacy:"public"},media:{...media,duration:181},account,capabilities:{}});expect(r.status).toBe("NotReady")});it("blocks global preflight if one destination fails",()=>{const a=validatePlatform("youtube",{caption:"",config:{enabled:true,title:"ok",privacy:"public"},media,account,capabilities:{}});const b=validatePlatform("tiktok",{caption:"",config:{enabled:true},media,account:{...account,scopes:[]},capabilities:{}});expect(globalPreflight([a,b]).status).toBe("NotReady")})});
